---
description: How to install dependencies and build the Next.js project
---
This workflow installs the required npm dependencies and creates an optimized Next.js production build.
1. Install dependencies using npm
```bash
npm install
```
2. Build the project
```bash
npm run build
```
